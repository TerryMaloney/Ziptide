# ZIPTIDE — Handoff: TC → T-dog (2026-06-15)

**From:** Terry's Claude Code (TC) · **To:** T-dog (wife's Unity-connected LLM, WL)
**Branch:** `terry-local-wip` · **Tip after this work:** `4870246` · **CI:** ✅ green (24 EditMode tests)

> **How to use this doc:** read it top-to-bottom for full context, then check §6 (what to verify),
> §3 (what to test on device), and §7 (the plan). Append your own entry to `docs/SESSION_LOG.md`
> when you start. The living contract is `docs/recipes/RECIPES.md` — read it first if you haven't.

---

## 1. Where things stand (the new situation)

- **GitHub + CI are healthy.** The Unity Personal license fight is over (the local `.ulf` is in the
  `UNITY_LICENSE` secret). CI compiles + runs EditMode tests on every push to `terry-local-wip` and is
  **green**. TC can read CI (`gh run list`) and re-push the `.ulf` to the secret if it ever expires again.
- **TC built the entire headless backbone this session** (persistence, registry, data model, idle
  economy) — all pure C#, all behind passing EditMode tests, **zero scene/rig/boot changes**. See §2.
- **One crash fix landed:** `ObjectiveBoard` NRE on every scene load (details in §3/§6).
- **Terry's local working tree still has uncommitted edits** TC deliberately did NOT touch:
  `D0_City.unity` (a full scene regen — see §8), `TestRoom_WorldPack.asset`, `AUDIT_REPORT.md`, `t.txt`
  (a Quest logcat, ignorable).

## 2. What TC shipped (review these — §6)

All on `terry-local-wip`, commits `70eff60` → `4870246`:

**Persistence** (`Core/Runtime/Persistence/`)
- `PlayerProfile.cs` — serializable save data (playerId, flags, resources, worlds), online-ready;
  `WorldState` now holds `mines[]` + `plots[]`.
- `ProfileSerializer.cs` — pure JSON serialize / deserialize / migrate; never throws on bad input.
- `SaveSystem.cs` (in `Gameplay/Runtime/Persistence/`) — DontDestroyOnLoad singleton.
  **NOT wired into `_Boot` yet** (that's report-only — see §5 proposal).

**Registry + data model** (`Content/Runtime/`)
- `Definition.cs` — abstract base for all new content defs (`id`, `displayName`).
- `DefinitionRegistry.cs` — generic `DefinitionRegistry<TDef>`: id→def, dup-guard, `Resources.LoadAll`
  auto-discovery (drop an asset, no central list → no merge collisions).
- `Definitions/` — `Resource/Tool/Machine/Plant/Creature/Biome/Recipe/BalanceConfig` + `ResourceCost`,
  all `[CreateAssetMenu]` under `Assets > Create > Ziptide/Definitions/…`.

**Idle economy** (`Core/Runtime/Economy/`)
- `IdleEngine.cs` — pure offline-accrual math (rate×time, cap, overflow, window clamp).
- `EconomyState.cs` — `MineState` (idle production into `stored`) + `PlotState` (time-based growth).
- `ProfileEconomy.cs` — `ResolveWorld(world, now, maxOffline)` advances mines + flags ready plots;
  `CollectMine` banks output into the profile.

**Tests** (`Tests/EditMode/`, new `Ziptide.Tests.EditMode` assembly — the project's first) — 24 tests:
ProfileSerializer (4), PlayerProfile (3), DefinitionRegistry (5), IdleEngine (6), ProfileEconomy (6).

**Docs:** `docs/recipes/RECIPES.md` (the consistency contract), `docs/SESSION_LOG.md` (shared log),
this handoff.

## 3. What Terry can TEST on device tonight

The backbone is CI-verified but **headless** — it has no in-game surface until wired (§5), so most of
it isn't on-device-testable yet. What IS testable now:

- [ ] **ObjectiveBoard fix** — load a scene with an ObjectiveBoard; confirm **no `NullReferenceException`
      in `ObjectiveBoard.CreateWorldSpaceText`** in logcat (it used to throw on every scene load).
- [ ] **Core-loop regression** — nothing TC changed should affect gameplay. Confirm the existing loop
      still works: `TRAVEL_OK`, movement (`MOVE_DIAG`), grab/holster, drone kill (`DRONE_DOWN`). If any
      regressed, it's unrelated to the backbone (TC touched no rig/travel/scene code) — capture logcat.
- [ ] **Step-offset error** — watch for `"Step Offset must be <= height + radius*2"` at
      `PlayerRigPersistence.cs:437` on travel (pre-existing rig bug; WL to fix — §7).

**Testable once §5 wiring is approved + built:** save persists across quit (`SAVE_OK`/`SAVE_LOAD` tags);
"welcome back" idle accrual after time away; resource collection from a mine.

## 4. Lane split + coordination (so we never collide)

- **TC** = headless backend C# (persistence, data model, economy, tests, tooling). CI-verified, no headset.
- **WL (you)** = editor/headset (scenes, patchers, XR rig, `_Boot`, art, on-device verify, Level 1).
- **File boundary:** TC owns `Content/Runtime/Definition*.cs`, `Content/Runtime/Definitions/*`,
  `Core/Runtime/Persistence/*`, `Core/Runtime/Economy/*`, `Tests/*`. **You own** all `.unity`,
  `Editor/Patching/*`, rig/`_Boot`, art, `ItemFactory.cs`, `InventoryState.cs`.
- **Shared files** (`ZiptideConstants.cs`, `WorldPackDefinition.cs`, build settings): additive only,
  claim in `docs/SESSION_LOG.md` first.
- **Rule:** pull before starting; small commits; log every session.

## 5. PROPOSAL (needs Terry's sign-off — report-only) — wire the backbone into the game

Three small hooks make save/idle actually run. They touch the boot/travel contract, so they wait for OK:

1. **Autosave on travel** — in `TravelCoordinator.TravelCoroutine` after `TRAVEL_START` (~line 89–96,
   beside the inventory save): `SaveSystem.Instance?.Save();`
2. **Resolve idle on arrival** — after the scene loads, before `TRAVEL_OK` (~line 150): get/create the
   destination `WorldState`, `ProfileEconomy.ResolveWorld(ws, nowUnix, balance.IdleCapSeconds)`, mark
   `discovered = true`, log `ZIPTIDE: IDLE_RESOLVED produced=… plotsReady=…`.
3. **Put `SaveSystem` in `_Boot`** — either add the component in the `_Boot` scene (WL, editor) or a
   runtime ensure in `BootLoader` (like BeltRig). Pick one; keep one owner.

Whoever does this: it's a single small commit, CI-compiled, then verified on device by the new tags.

## 6. What TC needs YOU to double-check (verify TC's work)

- [ ] Confirm CI is green on tip `4870246` (`gh run list --branch terry-local-wip` or the Actions tab).
- [ ] Open Unity → **Test Runner → EditMode**: confirm **24 Ziptide tests appear and pass**.
- [ ] Confirm **no compile errors/warnings** for the new scripts + the `Ziptide.Tests.EditMode` asmdef.
- [ ] **`.meta` files:** Unity will generate `.meta` for TC's new files on first import — **please
      `git add` and commit those new `.meta` files** so GUIDs are stable across our machines.
- [ ] Confirm the definitions show under **Assets > Create > Ziptide/Definitions/** (Resource, Tool, …).
- [ ] Sanity: **no new MonoBehaviour was added to any scene** (SaveSystem is intentionally unwired).
- [ ] Skim the data model vs `docs/design/SYSTEMS_ARCHITECTURE.md` — flag any field you'd want different
      before content gets authored against it (cheap to change now, expensive later).

## 7. The plan forward (roadmap)

Order from `SYSTEMS_ARCHITECTURE.md`, adapted to two agents:

- **Now (TC, headless):** ✅ Layer 0 persistence + ✅ Layer 1 data model/registry + ✅ idle engine. Done.
- **Next (needs §5 sign-off):** wire save + idle-resolve into boot/travel → first on-device save loop.
- **Then (TC):** live-world tick MonoBehaviour for the active world (thin wrapper over `ProfileEconomy`/
  machine defs); harvest v1 (node + tool → profile inventory); then mining/conveyor, garden.
- **Now (WL, editor/headset):** Level 1 Toxic Venice polish — your brief is the task list in §3/§7 of
  `docs/design/LEVEL1_TOXIC_VENICE.md`: verify ObjectiveBoard fix, fix step-offset + fall glitch, switch
  build to `PatchScenesThenAPK`, extend job chain, open one building, add ObjectiveBoard/waypoint guidance.
- **Then (both, feature-parallel):** each takes a whole world/creature/ship via the Recipes — e.g. TC a
  new world patcher, WL another / creatures vs ships. The backbone is now ready for this.

## 8. Open decisions for Terry

1. **`D0_City.unity` regen** — Terry's local copy is a full scene regeneration (every object renumbered +
   repositioned). Leave/commit/discard? WL: confirm in Unity whether the new layout is intended.
2. **§5 boot wiring** — approve so the save/idle loop runs on device?
3. **Build script** — switch `dev_build_install.ps1` → `BuildAndroid.PatchScenesThenAPK` (audit every build)?

---
*Generated by TC. Next handoff: `docs/handoffs/<date>_*.md`. Keep `docs/SESSION_LOG.md` current.*
