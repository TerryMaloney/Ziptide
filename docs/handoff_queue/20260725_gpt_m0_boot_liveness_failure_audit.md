### 2026-07-25 (gpt-m0-boot-liveness-audit) — M0 boot blocker diagnosed as mixed product + verification-net failure

- **Did:** reviewed Terry's device screenshots, live handoff, BootLoader, PlayerRigPersistence boot-hold/XRI paths, HomeHubRuntime, ComfortConsoleRuntime, UI readability audit, R1 integration-harness spec, exact c45b1a2 PlayMode artifact, boot smoke, synthetic tracked-rig helper, and travel round-trip test. Added `docs/recovery/M0_BOOT_MENU_DEADLOCK_DIAGNOSTIC_20260725.md`. Confirmed the global deadlock: intentional `BOOT_HOLD` disabled locomotion in floorless `_Boot`; Home Hub declared ready; zero active rays were present; required tiles were outside direct reach; no independent fallback existed. Confirmed the exact green c45 PlayMode artifact already logged `rays=0`, `NO_RAY_INTERACTORS`, `BOOT_HOLD on`, `HOME_HUB_READY`, and `BOARD_PROBE ray=none` before the test helper force-activated rays. The current PlayMode test then moved the ray to 1.1 m from the tile and directly invoked XRI manager selection, proving synthetic plumbing rather than production boot liveness. Classified `RED-CAUSE: mixed`.
- **Next:** preserve and review T-Dog's partial/uncommitted HomeHub diff before any competing edit. Upload and inspect the complete Quest log. Finish one narrow Home Hub candidate only under the recovery owner. Before another APK reaches Terry, require a production-initial-state `BOOT_LIVENESS` gate: while boot hold is active, at least one real ray, direct/poke, safe-locomotion, or explicit fallback escape path must exist from the actual tracked pose. Keep synthetic XRI plumbing as a separately named evidence class. Audit boot Settings/ComfortConsole, first W000 required verb, coupler/PRESS POWER, PUNCH IT/travel, and active-ray persistence after travel for the same all-exits-unavailable class.
- **Heads-up:** no code, scene, prefab, test implementation, build, artifact, or APK was changed. A placement-only menu fix may unblock the first screen while leaving zero production rays unresolved for later ray-only interactions. Do not release boot hold in floorless `_Boot` as a shortcut. The full Quest log is still required to determine whether `NO_RAY_INTERACTORS` persisted and whether additional lifecycle warnings occurred.
- **Commit:** `31da00aed16b40064b3e39b81ca54819c9d01df6`
