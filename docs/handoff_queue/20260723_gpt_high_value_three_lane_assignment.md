### 2026-07-23 (gpt-high-value-lanes) — next three lanes assigned for maximum Fable 5 leverage

- **Did:** Re-read current HANDOFF after T-Dog rb101 and Reasonbox rb102 completed. Claimed three non-overlapping high-value lanes. **GPT lane (start now): unified readiness authority** — extend the existing offline readiness output so durable CI, first-hour contracts, exact headset recovery, Meta release holds, and third-party licensing appear in one machine-readable answer with explicit deterministic blockers, awaiting-CI, awaiting-device, warnings, and release holds. **REASONBOX CLAIMED LANE: QUEST SYSTEM-FOCUS LIFECYCLE CONTRACT, source-audited and implementation-ready, no runtime edits.** Audit every existing lifecycle/input/audio/save/travel/job/coupler/multiplayer seam and create `docs/runtime_lifecycle/QUEST_SYSTEM_FOCUS_CONTRACT.md` plus `docs/runtime_lifecycle/quest_system_focus_contract.json`. Define one canonical lifecycle owner and exact state machine for Active, SystemOverlay, HeadsetRemoved, TrackingLost, Backgrounded, and Resuming; specify save timing, simulation pause, audio mute/duck, rendering continuity, controller/hand visibility, gameplay-input suppression, haptics, timers, travel/coupler/job atomicity, and offline/network behavior. Map each transition to current source paths and owners, identify displaced/duplicate ownership risks, and provide ordered implementation slices with EditMode/PlayMode/device acceptance evidence. Add a validator and mutation tests only if they enforce objective contract truth; do not create speculative runtime code. **T-DOG CLAIMED LANE: RELEASE BUILD HYGIENE AUDITOR, actual tooling not another prose checklist.** Audit BuildAndroid/editor scripts, Android PlayerSettings/manifest, per-platform define symbols, Development/Script Debugging flags, ARM64/IL2CPP, package/application IDs, versioning, permissions/features, keystore configuration, DevMenu/diagnostic stripping, Photon/PUN defines and AppId inclusion, demo-content inclusion, Platform SDK/entitlement readiness, and output size evidence. Create `docs/release/RELEASE_BUILD_PROFILE.md`, `docs/release/release_build_contract.json`, `tools/release_build_hygiene_gate.py`, mutation tests, and a Fast Preflight JSON report. Development mode may expose explicit release holds; a separate release mode must block false-ready claims. Do not change packages, runtime C#, Photon files, signing secrets, or the recovery APK.
- **Next:** GPT implements and verifies the unified readiness output. Reasonbox and T-Dog pull current `terry-local-wip` before starting, stay within their named paths, run the relevant full Python suites, and write their own Did/Next/Heads-up/Commit entries when finished. Their outputs should be detailed enough that a lower-cost coding model can execute the later runtime/build changes without repeating the expensive audit.
- **Heads-up:** The exact `c45b1a2` recovery card and APK remain frozen and run first. Reasonbox owns only new `docs/runtime_lifecycle/**` plus an optional dedicated lifecycle validator/test; T-Dog owns only new `docs/release/**`, `tools/release_build_hygiene_gate.py`, its tests, and the single Fast Preflight wiring/report line. Neither edits `docs/audio/**`, `docs/licensing/**`, store-readiness files, first-hour files, current runtime C#, recovery scenes, weapons, coupler, travel, mission-step classes, SunRig runtime, POI spawning, or handoff machinery. GPT owns `tools/offline_readiness_report.py` and its tests for this pass. If either discovers a necessary cross-lane change, record it as a dependency rather than editing outside ownership.
- **Commit:** this queued assignment entry; implementation commits and final verification follow. Do not claim latest source green until a durable verdict records it.
