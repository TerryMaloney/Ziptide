### 2026-07-23 (gpt-licensing) — licensing ledger enforced + T-Dog assigned a collision-free VRC/device-cert lane

- **Did:** Reconciled T-Dog/Fable C-lane's `STATE_OF_THE_GAME` rollup with the live branch. The durable CI verdict is now GREEN for source `96b22119b44c` (the state snapshot's cancelled-RED sentence is stale). Took the freeze-safe licensing gap: added authoritative `docs/CREDITS.md` + `docs/licensing/third_party_manifest.json`, `tools/third_party_license_gate.py`, eight mutation/production tests, and Fast Preflight evidence. The gate BLOCKS undeclared external roots/non-Unity packages, false release approval, missing license evidence, or unapproved content claiming to ship; known Photon metadata remains an explicit warning + distribution hold rather than guessed license facts. Added validated queued-handoff machinery (`tools/handoff_queue_apply.py`, tests, `.github/workflows/handoff-queue.yml`) so connector sessions can prepend entries without replacing/truncating this large file.
- **Next:** **T-DOG CLAIMED LANE — VRC/device-certification packet, docs-only and collision-free.** Pull current `terry-local-wip`, then: (1) correct only the stale CI sentence/status in `docs/STATE_OF_THE_GAME.md` using the current durable verdict; do not recensus or rewrite other pillars; (2) create `docs/VRC_TEST_PLAN.md`, re-verifying current requirements against official Meta developer sources; (3) expand only the certification/interrupt sections of `docs/DEVICE_TEST_CHECKLIST.md` with exact rows for first tracked frame within 4 seconds, Universal Menu/focus behavior, headset doff/resume at cold boot/home/held-item/coupler/travel/job/save, Guardian interruption, controller sleep/wake, offline start, thermal-load 72 Hz evidence, low-storage/save failure, update-over-install with an old save, and release-build permissions/manifest checks. Each row needs precondition, action, expected result, evidence/log artifact, and pass/fail field. Keep the exact `c45b1a2` recovery retry card unchanged; the new VRC matrix is for the broader post-recovery device pass. Run relevant docs/gate tests and record Did/Next/Heads-up/Commit in HANDOFF.
- **Heads-up:** **NO COLLISION BOUNDARY FOR T-DOG:** do not edit `docs/CREDITS.md`, `docs/licensing/**`, `tools/third_party_license_gate.py`, its tests, Fast Preflight, first-hour binding files, runtime C#, recovery/golden APK files, `docs/testing/HEADSET_RETRY_C45B1A2.md`, or handoff-queue machinery. GPT owns licensing follow-through and the stale first-hour evidence lane. Runtime art, multiplayer, weapons, coupler, travel, SunRig, mission-step classes, and POI spawning remain frozen until Terry's exact headset verdict. Photon is present but not release-approved until exact version/source/license obligations are proven.
- **Commit:** licensing source head through `a66c6c6c` plus this queued entry; final CI/Fast Preflight verdict must be checked after the queue write-back lands.
