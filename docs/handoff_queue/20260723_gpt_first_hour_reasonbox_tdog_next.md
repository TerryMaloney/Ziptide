### 2026-07-23 (gpt-first-hour) — first-hour evidence clean + permanent ratchet; Reasonbox audio lane and T-Dog follow-on assigned

- **Did:** Took the collision-free first-hour evidence reconciliation lane while T-Dog owns VRC/device certification. Added `tools/first_hour_evidence_refresh.py` plus eight mutation tests and `.github/workflows/first-hour-evidence-refresh.yml`. The applicator accepts a refresh only when the request identifies one exact beat/path/token, the stale token occurs exactly once in the inventory, the stale token is absent from current source, and the replacement exists in current source; it never edits runtime. Queued and applied four proven source-drift corrections: `FH_BOOT_READY` now follows BootLoader's `destination` callback variable (two tokens), and `FH_MACHINE_POWER_CYCLE` now follows `RepairStage.Running` plus the current multiline `MACHINE_REPAIRED` log prefix (two tokens). The queue was consumed only after `first_hour_binding_gate.py --strict` passed. Added the strict first-hour gate and its JSON artifact to Fast Preflight so future drift blocks instead of becoming background warnings. Runtime, recovery scenes, weapons, coupler, travel, and Golden APK route were untouched.
- **Next:** **REASONBOX CLAIMED LANE (start now) — AUDIO PRODUCTION PROGRAM, docs/research only.** Pull current `terry-local-wip`; audit all existing audio runtime, profiles, design docs, the single committed track, RILL text delivery, captions, and current licensing rules. Create only new files under `docs/audio/`: (1) `AUDIO_PRODUCTION_MASTER_PLAN.md`, covering the existing-vs-missing inventory, ZIPTIDE sonic identity, bus/mix/ducking architecture, spatialization rules, VR comfort and accessibility/caption requirements, file/import standards, Quest memory/voice budgets, naming/versioning, licensing-at-import workflow, production phases, and acceptance evidence; (2) `AUDIO_VERTICAL_SLICE_QUEUE.md`, an implementation-ready prioritized asset table for the first 20 minutes and M1-M4 (boot/home, RILL, comfort console, grab/holster, coupler repair stages, PUNCH IT/gate travel, W001 ambience, scanner/job, weapons/disable/salvage, creature presence, ship alerts/flight), with stable asset ID, trigger/runtime owner, mono/stereo, 2D/3D, loop/one-shot, target duration, priority, dependency, and device acceptance test. Reconcile with existing docs rather than inventing a parallel audio system. Do not generate/import audio, edit runtime, alter current audio assets, or edit licensing manifests; this lane defines the production program only. **T-DOG FOLLOW-ON LANE (only after the current VRC/device task is fully committed and handed off) — PHOTON PROVENANCE + RELEASE-LICENSE DECISION.** Pull again after finishing the current assignment. Inspect repository evidence under `Ziptide/Assets/Photon` and related package/demo metadata to identify the exact Photon product/SDK variant, version, source provenance, license/redistribution/attribution obligations, and whether any demo content is actually required by ZIPTIDE runtime. Verify current legal/product facts against official Photon sources; do not infer from folder names. Create `docs/licensing/PHOTON_RELEASE_DECISION.md` with evidence paths, exact findings, runtime dependency map, and a keep/strip/replace recommendation. Add durable evidence notes under `docs/licensing/evidence/photon/`. Update `docs/licensing/third_party_manifest.json` and `docs/CREDITS.md` only to the degree proven; if exact version/source/terms remain unprovable, preserve `hold-verification` and list the precise missing artifact needed. Run `tools/third_party_license_gate.py` and relevant tests. Do not delete Photon/demo files, change packages, touch multiplayer runtime, or mark distribution approved without evidence.
- **Heads-up:** Reasonbox owns only new `docs/audio/**` planning files. T-Dog remains on `docs/VRC_TEST_PLAN.md`, certification/interrupt sections of `docs/DEVICE_TEST_CHECKLIST.md`, and the narrow stale-CI correction in `docs/STATE_OF_THE_GAME.md` until that task closes; only then may T-Dog enter the Photon licensing follow-on. Neither operator edits first-hour files/tools/workflow, Fast Preflight, handoff-queue machinery, runtime C#, recovery/golden files, `docs/testing/HEADSET_RETRY_C45B1A2.md`, mission-step classes, SunRig runtime, POI spawning, weapons, coupler, or travel. GPT owns verification of the new first-hour and Fast Preflight CI results.
- **Commit:** first-hour refresh machinery through `46d0d4f`, applied inventory immediately after request `39e4540`, strict Fast Preflight ratchet `96c9761`, plus this queued handoff entry. Final durable CI verdict for the newest source must still be checked.
